#!/usr/bin/env bash

rm -rf /tmp/development
mkdir /tmp/development

cp lerna.json /tmp/development

for d in packages/* ; do 
    mkdir -p /tmp/development/$d
    cp $d/package.json /tmp/development/$d
    cp $d/package-lock.json /tmp/development/$d
    cp $d/CHANGELOG.md /tmp/development/$d
done