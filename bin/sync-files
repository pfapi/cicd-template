#!/usr/bin/env bash

set -e

git checkout development
git pull
bin/save-files
cp bin/apply-files /tmp
git checkout main
git pull
/tmp/apply-files
git add .
git commit -m "sync changed files by $GITHUB_JOB" || true
git push || true