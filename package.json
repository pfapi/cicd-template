{
  "name": "cicd-test",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "gen-package-locks": "lerna run --parallel gen-package-lock"
  },
  "devDependencies": {
    "lerna": "^6.4.0"
  },
  "dependencies": {
    "nx": "^14.5.2"
  }
}
