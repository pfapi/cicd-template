{
  "name": "cicd-test",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "gen-package-locks": "lerna run --parallel genPackagelock"
  },
  "devDependencies": {
    "lerna": "^5.3.0"
  },
  "dependencies": {
    "nx": "^14.5.2"
  }
}
