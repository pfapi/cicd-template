{
  "name": "cicd-test",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "gen-package-locks": "lerna run --parallel gen-package-lock"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "dependencies": {
    "nx": "^14.5.2"
  }
}
