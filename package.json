{
  "name": "cicd-test",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "gen-package-locks": "lerna run --parallel gen-package-lock"
  },
  "devDependencies": {
    "lerna": "^5.3.0"
  },
  "dependencies": {
    "nx": "^15.3.2"
  }
}
